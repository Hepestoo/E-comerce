// Este archivo se reemplaza automáticamente según la configuración de build.
// Los valores aquí son para PRODUCTION por defecto.

export const environment = {
  production: true,
  apiUrl: 'https://artemania-back.onrender.com'
};

// Para desarrollo local, descomentar estos valores:
/*
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'
};
*/
